[defaults]
inventory = /root/utility/sparta2/inventory
roles_path = /root/utility/ansible-roles
remote_user = root
log_path = /root/log
host_key_checking = False

[privilege_escalation]
become = true
become_user = root
become_method = sudo
become_ask_pass = false
